1. Run Build script
2. Run Insert script
3. Modify credentials.properties file
You must create a new user before logging in
The username must start with "E" for employees, everything else is assumed to be a customer
You can use the credentials E1/e1 (user/pass) to login (already in database) as an employee or C1/c1 for customer